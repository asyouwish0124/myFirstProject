spring:
  application:
    name: dictionary-v1
  cloud:
    config:
      uri: ${CONFIG_URI:http://112.124.6.220:8889}
      username: ${CONFIG_USERNAME:admin}
      password: ${CONFIG_PASSWORD:admin123}
      failFast: true
  profiles:
    active: local